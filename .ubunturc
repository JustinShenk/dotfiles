# vim: ft=bash

alias open="xdg-open"

export CCACHE_DIR=/local/.ccache

if [ -f /etc/bash_completion ]; then
    source /etc/bash_completion
fi

if [ -f $HOME/pass.bash_completion ]; then
    source $HOME/pass.bash_completion
fi

export PROJDIR=$HOME/Documents/Psiori/Projects/AutoCrane/autocrane-core
